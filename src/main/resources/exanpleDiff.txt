diff --git a/src/main/kotlin/pl/pietrzak/Application.kt b/src/main/kotlin/pl/pietrzak/Application.kt
index 5270bf8..63c58e2 100644
--- a/src/main/kotlin/pl/pietrzak/Application.kt
+++ b/src/main/kotlin/pl/pietrzak/Application.kt
@@ -37,8 +37,8 @@ fun main2() {
 }

 fun Application.mainModule() {
-    val clientId = System.getenv("GITHUB_CLIENT_ID") ?: error("GITHUB_CLIENT_ID not set")
-    val clientSecret = System.getenv("GITHUB_CLIENT_SECRET") ?: error("GITHUB_CLIENT_SECRET not set")
+    val clientId = System.getenv("GITHUB_CLIENT_ID") ?: ""//error("GITHUB_CLIENT_ID not set")
+    val clientSecret = System.getenv("GITHUB_CLIENT_SECRET") ?: ""//error("GITHUB_CLIENT_SECRET not set")

     install(CallLogging) {
         level = Level.INFO
diff --git a/src/main/kotlin/pl/pietrzak/webhook/GitHubWebhookRoutes.kt b/src/main/kotlin/pl/pietrzak/webhook/GitHubWebhookRoutes.kt
index f7a3465..24c618d 100644
--- a/src/main/kotlin/pl/pietrzak/webhook/GitHubWebhookRoutes.kt
+++ b/src/main/kotlin/pl/pietrzak/webhook/GitHubWebhookRoutes.kt
@@ -24,7 +24,7 @@ fun Routing.githubWebhookRoute() {
         if (event != "pull_request") return@post call.respond(HttpStatusCode.OK)

         val payload = call.receiveText()
-        println("payload")
+        println("payload : $payload")
         val json = Json.decodeFromString<GitHubPullRequestPayload>(payload)

         if (json.action == "opened" || json.action == "synchronize") {
